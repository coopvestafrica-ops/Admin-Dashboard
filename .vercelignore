# Exclude Next.js API routes (not needed - frontend uses Vite)
# Note: Keep /api/index.js for Vercel proxy
/api/admin

# Exclude Next.js public folder (not used)
/public

# Exclude Next.js shared libraries
/lib
