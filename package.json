{
  "name": "admin-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd artifacts/coopvest-dashboard && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
