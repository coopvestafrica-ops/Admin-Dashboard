{
  "name": "admin-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install lib/api-client-react && cd artifacts/coopvest-dashboard && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
