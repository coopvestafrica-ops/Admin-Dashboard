{
  "name": "admin-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/coopvest-dashboard run build"
  },
  "dependencies": {
    "@playwright/test": "^1.61.1",
    "@supabase/supabase-js": "^2.107.0",
    "playwright": "^1.61.1",
    "puppeteer-core": "^25.2.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "esbuild",
      "protobufjs",
      "tsx"
    ]
  },
  "devDependencies": {
    "vercel": "^54.18.6"
  }
}
