{
  "buildCommand": "pnpm --filter @workspace/coopvest-dashboard run build && mkdir -p api && cp -r api-serverless/members api/members",
  "outputDirectory": "artifacts/coopvest-dashboard/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "env": {
    "BASE_PATH": "/"
  }
}
