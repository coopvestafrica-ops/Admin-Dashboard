{
  "version": 2,
  "builds": [
    {
      "src": "artifacts/api-server/src/index.ts",
      "use": "@vercel/node"
    },
    {
      "src": "artifacts/coopvest-dashboard/dist/public/(.*)",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "artifacts/api-server/src/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "artifacts/coopvest-dashboard/dist/public/$1"
    }
  ],
  "installCommand": "cd artifacts/api-server && npm install --legacy-peer-deps && cd ../coopvest-dashboard && npm install && cd ../coopvest-dashboard && npm run build",
  "buildCommand": "cd artifacts/api-server && npm install --legacy-peer-deps && npm run build && cd ../coopvest-dashboard && npm run build"
}
