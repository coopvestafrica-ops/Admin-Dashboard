{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm install --ignore-scripts",
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/coopvest-dashboard run build",
  "outputDirectory": "artifacts/coopvest-dashboard/dist/public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api-server/dist/index.mjs", "headers": { "Content-Type": "application/json" } },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}